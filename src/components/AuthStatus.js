import { Box, Button } from '@mui/material';
import React, { useEffect } from 'react';
import { LoginOutlined } from '@mui/icons-material';

const AuthStatus = () => {
  return <Box mb={2} style={{ color: 'white' }}></Box>;
};

export default AuthStatus;
